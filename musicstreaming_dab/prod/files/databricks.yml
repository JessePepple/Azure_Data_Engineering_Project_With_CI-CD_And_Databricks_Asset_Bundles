# This is a Databricks asset bundle definition for musicstreaming_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: musicstreaming_dab
  uuid: f108f60a-e250-4fbe-8607-eecd5e1c9be9

include:
  - resources/*.yml
  - resources/*/*.yml



# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-2980095041369830.10.azuredatabricks.net
      presets:
        source_linked_deployment: false

    variables:
      catalog: musicstreaming_project
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-2980095041369830.10.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/jessepepple36@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/PROD/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: musicstreaming_project
      schema: prod
    permissions:
      - user_name: jessepepple36@gmail.com
        level: CAN_MANAGE
